---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-tls
spec:
  secretName: wildcard-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  commonName: "*.${DOMAIN}"
  dnsNames:
    - "*.${DOMAIN}"
    - "${DOMAIN}"
